import React from "react";
import ReactDOM from "react-dom/client";
import Traiter from "./Traiter.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Traiter />
  </React.StrictMode>
);
